curl url > file
curl https://mariolazzari.it
curl https://mariolazzari.it > mario.html

curl -X POST url
curl -d "this is a post body"
curl -X PUT -d "this is a put body"
curl -X DELETE

curl -b "name=mario" (cookie)

